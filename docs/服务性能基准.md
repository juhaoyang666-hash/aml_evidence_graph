# 服务性能基准

> 本报告仅表示固定本机环境下的可复现实测，不是生产 SLA。

- 完整状态：**true**
- 硬件边界：Windows local development host, 16 logical CPUs; no production traffic.

| 路径 | 请求或周期/并发 | 错误率 | 吞吐（路径单位/s） | p50 ms | p95 ms | p99 ms |
|---|---:|---:|---:|---:|---:|---:|
| Mock 健康检查 (`GET /healthz`) | 100/5 | 0.00% | 883.39 | 4.11 | 13.60 | 18.13 |
| Mock 调查草稿 (`POST /demo/cases/mock-alert-0001/draft`) | 50/5 | 0.00% | 80.88 | 57.95 | 123.48 | 140.38 |
| 冻结 CatBoost v1 分区评分 (`POST /v1/score/batch`) | 10/1 | 0.00% | 1.85 | 547.23 | 633.43 | 652.36 |
| 受控 Agent 查询/恢复 (`POST+GET+POST /v1/controlled-investigations/{alert_id} -> status -> review`) | 50/5 | 0.00% | 49.73 | 93.12 | 147.16 | 155.85 |
| Agent 幂等探针（每周期 8 个并发同键审批） (`POST+GET+POST /v1/controlled-investigations/{alert_id} -> status -> review`) | 50/5 | 0.00% | 8.99 | 526.97 | 668.67 | 849.61 |

## Linux 冻结 GAT / 融合补充基准

> 环境：Linux，RTX 3090 `cuda:3`；公开合成 SAML-D 的 2023-07-01 分区。
> 这两项是进程内批分区实测，不包含 HTTP/网络，不是生产 SLA。

| 路径 | 行数/告警 | 重复 | p50 | p95 | 资源 |
|---|---:|---:|---:|---:|---|
| 单独 GAT forward（快照预构建） | 28,539 行 | 3 | 104.99 ms | 98.20 ms（稳态） | GPU allocated 214 MiB |
| CatBoost + GAT + 冻结融合 + 告警证据 | 28,539 行 / 145 告警 | 3 | 13.01 s | 13.86 s | RSS 3.33 GiB；GPU 214 MiB |

完整评分服务在首次基准中暴露了两个问题，均已加回归保护：

1. 旧实现在阈值筛选前对全部 28,539 笔交易逐笔扫描近 90 万历史边生成证据。
   现在每个快照只建一次历史邻接索引，且只对 145 个最终告警生成证据。
2. 旧图推理每次请求使用不同的全局随机状态采样邻居，导致重复评分告警数不一致。
   现在推理采样固定为 checkpoint `random_seed`，三次告警数均为 145。

聚合产物见 `artifacts/serving_benchmark_gat_v1/summary.json` 与
`artifacts/serving_benchmark_gat_fusion/summary.json`。

## 解读边界

- Mock 路径不加载冻结模型，不能代表模型推理延迟。
- Windows HTTP 和 Linux 进程内基准的来源已齐全；口径不同，不得直接混合比较，
  且均不代表生产 SLA。
- 报告不包含真实交易、账户标识、Token 或外部服务密钥。
- 冻结评分使用 v1 CatBoost 与 2023-07-01 完整 PIT 日期分区，不含 GAT 或融合；吞吐单位是分区/秒，不是交易/秒。
- 受控 Agent 指标是一个启动、查询、审批恢复周期的端到端延迟。
- 幂等探针共发出 400 个 review 请求，其中 350 个为持久化回放；独立审计库仅有 50 条 review 与 50 条 finalize。该保证限于单 API 进程，不代表多 worker exactly-once。
