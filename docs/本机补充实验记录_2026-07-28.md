# Linux 本机补充实验记录

> 日期：2026-07-28  
> 环境：`/data1/yangjuhao/envs/risk`  
> 数据边界：公开合成 SAML-D，不是真实业务流量。

## 已完成

### 1. FE v2 CatBoost + 历史 GAT v1 融合

- 冻结测试 PR-AUC：`0.9576137`
- 历史 GAT v1 PR-AUC：`0.9483330`
- 配对差值：`+0.0092807`
- 200 次配对分层 Bootstrap 95% CI：`[+0.0035747, +0.0162051]`
- 融合自身 95% CI：`[0.9504712, 0.9641203]`
- 0.1% / 0.5% / 1% 预算分别比 GAT 多找到 `17 / 10 / 7` 个正例。

这与 Windows 上的 GAT 本地重训版对照不同：当时融合相对本地重训 GAT
为 `-0.00171`，区间跨 0。这说明融合结论对 GAT checkpoint / 训练波动敏感，
不应只披露更有利的一组。

### 2. 真实 GAT checkpoint 加载与 GPU 推理

已修复加载器固定实例化 GraphSAGE 的问题，现在按 checkpoint
`config.architecture` 构造 GraphSAGE / GAT / RGCN / PNA，并在 RGCN 评分快照中
保留 `history_edge_type`。

使用历史 `artifacts/gat/graphsage.pt` 在 `cuda:3` 对 2023-07-01 分区实测：

| 项目 | 结果 |
|---|---:|
| 当日评分行数 | 28,539 |
| 30 天历史行数 | 897,243 |
| checkpoint 加载 | 1.45 s |
| 快照读取+构建 | 8.79 s |
| 首次 CUDA 推理 | 0.758 s |
| 稳态推理均值 | 97.18 ms |
| 稳态吞吐 | 293,669 rows/s |
| GPU peak allocated / reserved | 214 / 232 MiB |

口径为本机冻结分区实测，不是生产 SLA。端到端主要开销在快照读取/构建，
不在稳态 GAT forward。聚合报告见
`artifacts/serving_benchmark_gat_v1/summary.json`。

### 3. 环境与回归

- 补齐 `langgraph-checkpoint-sqlite 3.1.0`、`sentence-transformers 5.6.1`、
  `mlflow 3.14.0`、`pyspark 4.2.0`。
- 在 `risk` 环境内安装 OpenJDK `17.0.14`；PySpark 本地 `range(3).count()` 作业通过。
- `pip check` 无冲突；`ruff check src tests` 通过。
- pytest：`117 passed, 1 skipped`。
- Linux 权威融合拟合/测试已同步到本地 MLflow experiment
  `aml-evidence-graph-linux-authoritative`，仅记录聚合指标和 manifest。
- 自动简历证据门禁已通过，`docs/简历证据.md` 为 `public_ready=true`。
- 门禁对账确认本机 FE v2 GAT / 融合为 `0.93887 / 0.95540`；旧文档的
  `0.90937 / 0.93193` 来自另一台机器，不是本机这两个 run。

### 4. 第二随机种子与拒答冻结测试

- GAT 第二 seed `20260723` 已完成：验证 / 测试 PR-AUC 为
  `0.90693 / 0.92949`；相对首 seed 测试结果下降 `0.01884`，因此不晋升 sidecar。
- 检索拒答使用 v2 的 80 案校准、v3 的 50 案冻结测试，case ID 不重叠。
  阈值仍为 `0.35`；校准/测试无答案误召回率为 `0.200 / 0.500`，说明单阈值未泛化。

### 5. 完整 GAT + CatBoost + 融合评分

对 2023-07-01 的 28,539 笔交易做三次进程内冻结评分，均产生 145 个告警；
p50/p95 为 `13.01 / 13.86 s`。期间修复了两项问题：图证据改为历史索引一次构建并仅对
最终告警生成；推理邻居采样每次从 checkpoint seed 开始，重复评分完全一致。

## 当前运行状态

GAT 工程 Pareto 已完成。实验固定 seed `20260722`，六个候选只读取 train/validation；
在 GPU3/GPU0 最多两路并行，比较 batch size `1024/4096`、fanout
`(10,5)/(25,15)` 和历史窗口 `14/60` 天。60 天窗口 validation PR-AUC 最高，为
`0.92423`，相对 30 天基线 `0.91788` 增加 `0.00634`，已按预注册规则选为唯一冻结测试候选。
唯一冻结测试 PR-AUC 为 `0.95206`，相对 30 天 GAT `0.94833` 点差 `+0.00373`；
200 次配对 Bootstrap 95% CI 为 `[-0.00147, +0.00915]`，不支持显著提升；
0.1% 告警预算 Precision / Recall 为 `0.9808 / 0.8434`。完整测试快照物化耗时
约 `29.18 min`、RSS 峰值约 `10.32 GiB`，图推理本身约 `47.03 s`。

风险切片显示任一端点新账户 / 低度非零 PR-AUC 为 `0.7915 / 0.6070`，两端点已见 /
高度交易为 `0.9863 / 0.9936`；跨境和换汇均约 `0.9756`。后续优先补新账户、低度节点和
弱 Typology 特征，而不是继续堆跨境规则。

四组 leave-one-family-out 验证集实验已在 `tmux: aml_gat_feature_ablation` 两路启动，
固定 60d 配置，只移除金额、时间窗口行为、relationship 或 graph 节点统计之一，不读取 test。

二阶段检索拒答也已完成 v2 五折 OOF：Recall@3 / 无答案误召回率为
`0.700 / 0.067`；v3 回顾诊断为 `0.905 / 0.000`。由于 v3 已经促成方案设计，不能再作为
独立冻结测试，门控保持实验性并等待新盲审集。

完整融合 HTTP 两 worker 基准也已完成：4 个请求、并发 2、错误率 `0%`，p50/p95
`33.83 / 34.85 s`，吞吐 `0.059 req/s`。跨连接证据探针 20 次读取出现 1 次 `404`，
证明当前内存 evidence store 不具备多 worker 一致可见性。压测客户端已默认禁用环境代理，
避免本机 `HTTP_PROXY` 将回环地址错误送入代理。

## 仍待补齐

1. 金额、时间、关系、节点统计四组特征族消融，重点观察新账户/低度切片。
2. 为二阶段拒答新增独立盲审集，不再用已观察的 v3 调参。
3. 多 worker Agent/evidence store 的共享存储、CAS/锁和 exactly-once 验证。
