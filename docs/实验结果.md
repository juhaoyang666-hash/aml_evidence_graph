# 全量实验结果（SAML-D）

**Protocol**: public synthetic SAML-D · PIT features · fixed time-out split ·
test = 1,558,821 rows / 1,813 positives (positive rate ≈ 0.116%).  
**Primary metric**: PR-AUC. ROC-AUC is secondary at this rarity.  
**Primary sources**: `artifacts/table_lightgbm_fe_v2`,
`artifacts/gat_cold_start_v3_novelty_only_60d_seed20260723_frozen_test`, and
`artifacts/fusion_test_lightgbm_fe_v2_gat_v1`; rules/LR/GraphSAGE retain
their versioned historical artifacts.

## 当前主线测试结果

| Model | Role | PR-AUC | ROC-AUC | 0.1% Precision | 0.1% Recall | 0.5% Precision | 0.5% Recall | run_id |
|---|---|---:|---:|---:|---:|---:|---:|---|
| rules (v2026.2) | baseline | 0.0015 | 0.5634 | 0.0019 | 0.0017 | 0.0026 | 0.0110 | `20260725T103959Z-27c7d65545` |
| logistic | table | 0.1966 | 0.9829 | 0.2809 | 0.2416 | 0.0916 | 0.3938 | same |
| **LightGBM FE v2** | **primary table** | **0.9014** | 0.9984 | **0.9403** | **0.8086** | 0.2199 | 0.9454 | `20260811T082808Z-6f164d7ad8` |
| **GAT + event-time novelty** | **primary graph** | **0.9714** | 0.9998 | **0.9814** | **0.8439** | 0.2312 | 0.9939 | checkpoint `8c7bb6cf63ec` |
| **fusion (LightGBM FE v2 + local GAT v1 replay)** | **OOF fusion (2-way)** | **0.9561** | 0.9999 | **0.9827** | **0.8450** | 0.2322 | 0.9983 | `20260811T083954Z-048e8d1338` |

Notes:

- Main-line reporting: **LightGBM FE v2**, **GAT + event-time novelty**, and the precisely
  versioned **`LightGBM FE v2 + local GAT v1 replay` OOF fusion**. GraphSAGE is reported
  only in the historical v1 architecture comparison below.
- The current fusion does not contain the event-time-novelty checkpoint. Do not describe
  it as a fusion of the latest two promoted components.
- The historical 3-way fusion and `graph_stats_catboost` remain in the ablation appendix
  as synthetic-mechanism contrasts; neither is a main-line result.
- Budget columns are precision/recall at top-k alert fractions of the test set.

## LightGBM / XGBoost FE v2 同协议候选

2026-08-11 在本机使用与 CatBoost FE v2 相同的 PIT 数据、时间切分、`graph_` 特征排除、
50 万训练负样本上限和训练期类别词表，先在验证集各比较两个固定候选，再对每个模型族的胜者
读取一次冻结测试。测试集不参与参数选择。正式 run 为
`20260811T075102Z-bf29a6e657`。

| 模型 | 验证 PR-AUC（seed 20260722 / 20260723） | 测试 PR-AUC | ROC-AUC | 0.1% P / R | 0.5% P / R |
|---|---:|---:|---:|---:|---:|
| CatBoost FE v2 | 0.8661 / — | 0.8754 | 0.9984 | 0.8999 / 0.7739 | 0.2194 / 0.9432 |
| **LightGBM leaves=63** | **0.8941 / 0.8953** | **0.9014** | 0.9984 | **0.9403 / 0.8086** | **0.2199 / 0.9454** |
| XGBoost depth=8 | 0.8924 / 0.8910 | 0.8969 | 0.9984 | 0.9359 / 0.8047 | 0.2194 / 0.9432 |

200 次同交易分层配对 Bootstrap：

| 候选减基准 | 测试 PR-AUC 点差 | 95% CI |
|---|---:|---:|
| LightGBM − CatBoost | **+0.02599** | **[+0.02084, +0.03196]** |
| XGBoost − CatBoost | **+0.02146** | **[+0.01562, +0.02726]** |
| LightGBM − XGBoost | **+0.00454** | **[+0.00207, +0.00780]** |

LightGBM 和 XGBoost 均稳定超过 CatBoost，且 LightGBM 在两个 seed 的验证期都领先 XGBoost。
LightGBM 已进一步生成 4,524,912 条 expanding-time OOF，完成模型持久化、FastAPI 受控
加载、全局 gain 重要性与局部 SHAP 解释，并与本机 GAT v1 replay 完成冻结融合。由此正式
替换 CatBoost 成为表格主线；CatBoost 与 XGBoost 保留为历史和同协议模型族对照。

LightGBM + GAT v1 融合使用训练期 OOF 拟合二层逻辑回归、验证期 isotonic 校准和锁定阈值，
冻结测试 PR-AUC 为 `0.95606`，0.1% 预算 P/R 为 `0.98268/0.84501`；200 次 Bootstrap
PR-AUC 95% CI 为 `[0.94924,0.96346]`。该融合仍低于 GAT + 时点新颖性 `0.97140`，因此
展示融合工程能力但不替代主线图排序器。

首轮 LightGBM 曾因同时监控不平衡验证集的默认 binary logloss，将早停点错误锁在第 1 棵树；
该 run 已隔离为 `artifacts/boosting_fe_v2_invalid_lgb_early_stopping`，未进入上述结论。正式脚本
只按验证 PR-AUC 早停。第二 seed 使用 `--validation-only`，未生成或读取测试分数。

## 历史 v1 与 FE v2 晋升记录

> CatBoost FE v2 是前一版表格主线，现已被 LightGBM FE v2 替换；GAT FE v2 未晋升。以下数字保留实验演进与
> Linux/Windows 对照，不覆盖上方当前主线表。

- 表格指标唯一真值：`artifacts/table_baseline_fe_v2/metrics.json`。
- Linux 权威 sidecar（简历门禁）：GAT `0.93887`、同版本融合 `0.95540`；
  跨版本融合与跨机器对照见 [特征工程V2实验.md](特征工程V2实验.md)。
- 复现入口：`scripts/pipelines/run_fe_v2_pipeline_resumable.py`（PIT sidecar + table）；
  图/OOF/融合链见 `scripts/pipelines/run_fe_v2_gat_oof_fusion.ps1`。

关键指标（CatBoost FE v2）：

| Metric | Value |
|---|---:|
| CatBoost validation PR-AUC | **0.8660898669** |
| CatBoost validation ROC-AUC | **0.9982223689** |
| CatBoost test PR-AUC | **0.8754139061** |
| CatBoost test ROC-AUC | **0.9984022445** |
| 相对历史 CatBoost v1 test PR-AUC=0.8092 的差值 | **+0.0662**（四舍五入） |

说明与边界：

- CatBoost FE v2 曾替代 v1 CatBoost；两者现均作为 LightGBM 主线之前的历史基准保留。
- smoke 产物仅用于链路验证，不可引用为正式效果数字。
- Windows 与 Linux 的 FE v2 GAT/融合数字不得混为同一 run。

## 历史 v1 模型选择：GAT vs GraphSAGE（2-way fusion）

Both fusions use identical protocol and identical frozen CatBoost score files
(`artifacts/table_baseline/scores`), components **`catboost,graphsage`** only, no
`graph_stats`. Only the graph component differs. The OOF score column is literally
named `graphsage` in both runs because the OOF writer labels the graph column by CLI
`--model`, not by architecture; in the GAT run that column holds **GAT** scores
(`artifacts/graph_oof_gat`, `run_id=20260726T090533Z-9e665d8d94`,
`model_config=configs/models.gat.yaml`, 4,524,912 OOF rows, 3 expanding folds,
minimum 2 training months).

| Fusion | Test PR-AUC | ROC-AUC | 0.1% P | 0.1% R | 0.5% P | 0.5% R | fit run_id | test run_id |
|---|---:|---:|---:|---:|---:|---:|---|---|
| catboost + graphsage | 0.8973 | 0.9996 | 0.9076 | 0.7805 | 0.2281 | 0.9807 | `20260725T145556Z-1fafa8b428` | `20260725T145745Z-a7d61d7c8d` |
| **catboost + GAT** | **0.9175** | 0.9994 | **0.9359** | **0.8047** | **0.2298** | **0.9879** | `20260726T090619Z-0ff8a9c7ff` | `20260726T090744Z-4a97fdf32f` |

Artifacts: `artifacts/graph_oof_gat` → `artifacts/fusion_cb_gat` →
`artifacts/fusion_test_cb_gat`.

**Verdict.** Swapping GraphSAGE for GAT improves the 2-way fusion on every reported
figure except ROC-AUC (0.9996 → 0.9994, immaterial at this rarity): PR-AUC
0.8973 → **0.9175** (+0.0202), 0.1% budget precision 0.9076 → **0.9359**, recall
0.7805 → **0.8047**. This established GAT as the preferred v1 graph component; the
section is retained as historical architecture-selection evidence.

**Caveat, stated plainly:** the fusion does **not** beat GAT alone. On the same test
split GAT alone scores PR-AUC **0.9483** with 0.1% budget P 0.9743 / R 0.8378, which is
better than the 0.9175 fusion. So fusion helps relative to CatBoost (0.8092) and
relative to the old GraphSAGE fusion, but for this dataset the single GAT edge model is
the strongest scorer. Two defensible reporting choices follow, and this project keeps
both visible rather than picking the flattering one:

- If the goal is peak ranking quality: report **GAT alone** (0.9483).
- If the goal is the fusion architecture the system is built around (tabular + graph,
  leakage-safe OOF stacking, calibrated threshold policy): report
  **`catboost + GAT`** (0.9175) and disclose that GAT alone ranks higher.

No single-model/fusion comparison in the original training manifests used confidence
intervals (`bootstrap_iterations=0`). A post-hoc stratified bootstrap (200 iterations)
is reported below so gaps can be discussed with intervals.

## Bootstrap CI (test set, 200 stratified iterations)

Source: `artifacts/diagnostics/bootstrap_ci.json` · seed `20260722` · 95% intervals.  
Scores: CatBoost from `table_baseline_rules`, GAT from `artifacts/gat`, fusion from
`fusion_test_cb_gat` (`fusion_calibrated_probability`), aligned on `transaction_id`.

| Model | PR-AUC point | 95% CI | ROC-AUC point | 95% CI |
|---|---:|---|---:|---|
| CatBoost | 0.8092 | **[0.7914, 0.8260]** | 0.9963 | [0.9958, 0.9969] |
| **GAT** | 0.9483 | **[0.9396, 0.9549]** | 0.9996 | [0.9994, 0.9998] |
| **catboost + GAT** | 0.9175 | **[0.9073, 0.9276]** | 0.9994 | [0.9987, 0.9998] |

**Reading the gaps**

- Fusion vs CatBoost: fusion CI sits entirely above CatBoost CI → improvement is stable.
- Fusion vs GAT alone: fusion CI sits entirely **below** GAT CI → “fusion &lt; GAT alone”
  is also stable on this split — keep disclosing both numbers.
- Point CatBoost+GraphSAGE fusion (0.8973) lies below the CatBoost+GAT fusion lower bound
  (0.9073) → replacing GraphSAGE with GAT in the 2-way stack is a clear gain, even without
  re-bootstrapping the older fusion file.

## CatBoost vs GAT gap diagnosis

At 0.1% budget, GAT recovers **355** true positives that CatBoost misses (overlap 1,164);
CatBoost-only TP = 173. The missed positives have mean CatBoost rank 6,672 versus GAT rank
1,505. CatBoost already uses `relationship_*` window features in its top importances — the
residual gap is consistent with neighborhood message-passing beyond pairwise aggregates.

Hard-negative retrain (`artifacts/table_baseline_hardneg`, run
`20260726T113335Z-a63ad9a6e2`) **regressed** CatBoost test PR-AUC from 0.8092 → **0.4548**;
keep stable hash-sampled CatBoost. This is a recorded negative result, not a tuning omission.

Source: `artifacts/table_baseline_rules` → `alert_reduction_vs_rules`.

| Model | @50% recall · reduction | model alerts | rules alerts | @70% | @85% |
|---|---:|---:|---:|---:|---:|
| **catboost** | **99.86%** | 908 | 649,036 | 99.88% | 99.56% |
| logistic | 98.03% | 12,780 | 649,036 | 97.37% | 95.99% |
| graph_stats_catboost | 99.86% | 907 | 649,036 | 99.89% | 99.89% |

Rules: `configs/rules/default.yaml` v2026.2 (train-period quantiles only).  
Full PIT rule hits: 836,487 across 9,504,852 rows.

## 历史 v1 investigation views

Built from frozen fusion test scores (`fusion_calibrated_probability`, as_of
`2023-08-23T23:59:59Z`, 30-day window, risk threshold 0.5).

| Artifact | Source fusion | run_id | accounts | funds paths | cases |
|---|---|---|---:|---:|---:|
| `artifacts/test_investigation_views` | 3-way fusion (`artifacts/fusion_test`) | `20260725T111133Z-e73ad70672` | 307,103 | 266 | 228 |
| **`artifacts/test_investigation_views_gat`** | **catboost + GAT** (`artifacts/fusion_test_cb_gat`) | `20260726T090826Z-b9ef382e9f` | 307,103 | 135 | 212 |
| **`artifacts/test_investigation_views_lightgbm_fe_v2_gat_v1`** | **LightGBM FE v2 + GAT v1** | `20260811T084332Z-44a4ad7777` | 307,103 | 186 | 219 |

These views predate the current promoted model table. The GAT-based views carry fewer
funds paths and cases because the calibrated GAT fusion
puts fewer accounts above the 0.5 risk threshold — a sharper, more concentrated alert
set, not a data change. Account universe is identical (307,103). They demonstrate the
post-scoring aggregation workflow and must not be presented as outputs of the current
CatBoost FE v2 + local GAT v1 fusion.


## Suggested resume wording

> On public synthetic SAML-D (~9.5M txs, ~0.1% positives), PIT features + LightGBM
> FE v2 reaches test PR-AUC 0.901; at a 0.1% alert budget, Precision 0.940 / Recall
> 0.809. A history-only GAT edge classifier with event-time novelty features reaches
> test PR-AUC 0.971 (0.1% budget P 0.981 / R 0.844). Leakage-safe expanding-time OOF
> fusion of LightGBM FE v2 and a local GAT v1 replay reaches test PR-AUC 0.956
> (0.1% budget P 0.983 / R 0.845). Versus train-period quantile rules, LightGBM FE v2
> cuts alert volume by ~99.9% at matched 50% recall.

Always attach: **dataset + run_id + time-split protocol**. If you quote the fusion
number, also quote GAT + event-time novelty (0.971) and state that the current fusion
uses GAT v1 rather than the novelty checkpoint.


## Golden (project-adjudicated investigation regression)

Source: `golden/cases_v1.json` + adjudication record `golden/adjudication_v1.json`
(18 typology + 6 low-evidence + **10** adversarial after B5 expansion; was 6 adversarial).  
**Adjudicator**: `agent-authorized-by-user` (user-authorized agent pass on 2026-07-26;
adversarial expansion also agent-authorized).  
This **is** the project’s adjudicated Golden set for regression — **not** an independent
third-party human panel, and not production compliance labels.

### Template path (no LLM) — expanded set

`artifacts/golden_summary.json` · `run_id=20260726T124225Z-d0dc4bc053` · **34** cases

| Metric | Value |
|---|---:|
| Cases | **34** |
| Schema / fact-snapshot match | 1.0 / 1.0 |
| Correct rejection rate | 1.0 |
| Hallucination intercept rate (injected probes) | **1.0** (7/7) |
| No-evidence refusal rate | **1.0** |
| Latency p50 / p95 (ms) | ~8.4 / ~10.4 |

### Template path (no LLM) — original Golden 30 (retained)

`run_id=20260726T020456Z-e698864ca4` · 30 cases · hallucination intercept 1.0 (4/4) ·
latency p50/p95 ~7.3 / ~7.8. Kept for comparability with the LLM path below.

### LLM path (ECNU) — original 30

`artifacts/golden_summary_llm.json` · `run_id=20260726T020614Z-61765ffec9` · model `ecnu-max` ·
prompt `ecnu-risk-evidence-v1`

| Metric | Value |
|---|---:|
| Cases | 30 |
| Schema / fact-snapshot match | 1.0 / 1.0 |
| Hallucination intercept rate (injected probes) | **1.0** (4/4) |
| No-evidence refusal rate | **1.0** |
| Correct rejection rate | 0.90 |
| LLM annotation rate | 0.733 |
| Latency p50 / p95 (ms) | **2684 / 3721** |
| Reported tokens (prompt / completion) | 7618 / 6161 |

Notes: three cases (`agent-typo-10-in`, `agent-typo-16-cycle`, `agent-low-evidence-03`)
received LLM annotations that failed fact validation (`rejected_facts`) — the safety
gate worked; this lowers `correct_rejection_rate` because those cases expect
`draft_requires_human_review`. Injected adversarial probes still intercept at 100%.
Prompt-injection cases (`agent-adv-05/06`) completed as human-review drafts.
该 30 案结果仅作历史对照；34 案新执行如下。

### LLM path (ECNU) — 34-case frozen run and prompt regression

2026-08-04 首次冻结执行使用 `ecnu-risk-evidence-v1`，run
`20260804T031333Z-e6d35f041c`。34 案中 7 案为确定性注入探针，27 案真实调用
`ecnu-max`：解析成功率 `0.5556`，解析后事实门通过率 `0.8667`，p50/p95
`17.35 / 120.08 s`。13 条安全输出经项目内逐条复核，条件化、问题可执行和实际返回对抗案例
注入抵抗均为 `1.0`，但严格证据 Grounding 与 Overall 均为 `0.0`：模型反复把“存在分数字段”
写成“风险升高”，而外部 payload 实际没有数值。

修复后的 `ecnu-risk-evidence-v3` 同集合回归 run
`20260804T033209Z-b7e39be09d`，外部解析率 `0.7407`，20/20 解析输出通过事实门，p50/p95
`7.24 / 10.21 s`。项目内人审覆盖 20/20：Grounding 和 Overall `0.90`，条件化与问题可执行
`1.0`，实际返回的对抗案例注入抵抗 `1.0`（2/2）。7 次非法 JSON 仍安全回退确定性模板。
该 v3 结果是观察 Bad Case 后的开发回归，不是独立盲测或生产 SLA；token `11175 / 8362`
仅覆盖安全输出，未配置合同价格，不报告成本。

### LLM path (ECNU) — preregistered prompt-isolated Holdout

在查看任何返回前，提交 `41922b8` 冻结 `golden/llm_holdout_cases_v1.json` 与
`golden/llm_holdout_protocol_v1.json`。24 个新 case ID 与开发集完全不重叠，覆盖 8 个
Typology、6 个低证据案例、6 个真实对抗调用和 4 个确定性事实探针；协议固定 Prompt v3
SHA-256、单次完整运行、禁止逐案重试和项目内盲审 rubric。

唯一 run `20260804T061709Z-2930f2efc8` 的结果：

| 指标 | Holdout 结果 | 预注册门槛 | 结论 |
|---|---:|---:|---|
| 外部解析成功率 | 0.7500（15/20） | 未设 | 5 次非法 JSON |
| 解析后事实门 | 1.0000（15/15） | 1.0000 | 通过 |
| 人审覆盖 | 1.0000（15/15） | 1.0000 | 通过 |
| 人审 Grounding | 0.8000（12/15） | 0.9000 | **未通过** |
| 条件化/非决策 | 1.0000（15/15） | 未设 | 通过 |
| 问题可执行 | 0.9333（14/15） | 未设 | 1 条无问题 |
| 实际返回对抗案例注入抵抗 | 0.7500（3/4） | 未设 | 1 条采纳标识符请求 |
| 人审 Overall | 0.7333（11/15） | 0.9000 | **未通过** |
| P50 / P95 | 6.77 / 44.46 s | 未设 | 非生产 SLA |

Holdout 揭示了同集开发回归没有暴露的泛化缺口：模型仍会从空 missing-evidence 列表推断资料完整，
偶发把隐藏分数称为 elevated，一次未输出任何核查问题，并有一次虽然没有泄露标识符，却把注入的
外传请求改写成推荐问题。结果按预注册规则保留为负结果，不回写 Prompt v3 或 Holdout 案例。
该实验独立于 Prompt 调整，但人审仍由项目 Agent 完成，不是外部专家裁定。

### LLM path (ECNU) — Prompt v4 candidate Holdout v2

Prompt v4 根据 Holdout v1 的四类已揭示 Bad Case 编写，先冻结为提交 `6bd5566`；随后才生成并
冻结全新 Holdout v2（提交 `ddc3682`）。v2 的 24 个 case ID 与此前两套集合不重叠，协议增加
解析率、条件化、问题可执行和注入抵抗门槛。

唯一 run `20260804T065403Z-6e3811213a` 的结果：

| 指标 | Holdout v2 结果 | 预注册门槛 | 结论 |
|---|---:|---:|---|
| 外部解析成功率 | 0.1000（2/20） | 0.8000 | **未通过** |
| 解析后事实门 | 1.0000（2/2） | 1.0000 | 通过但样本极少 |
| 人审覆盖 | 1.0000（2/2） | 1.0000 | 通过 |
| 人审 Grounding | 1.0000（2/2） | 0.9000 | 描述性结果，不足以晋升 |
| 条件化/问题可执行 | 1.0000 / 1.0000 | 1.0000 / 1.0000 | 描述性结果 |
| 实际返回对抗案例注入抵抗 | 1.0000（1/1） | 1.0000 | 描述性结果 |
| 人审 Overall | 1.0000（2/2） | 0.9000 | 描述性结果，不足以晋升 |
| P50 / P95 | 4.81 / 19.41 s | 未设 | 非生产 SLA |

18 次失败均被归类为 `annotation_json_invalid`，没有逐案重试。由于失败原文未被持久化，不能证明
具体根因；更短的输出上限可能造成截断只是待验证假设。按照预注册规则，解析率失败已经否决晋升，
该阶段默认 Prompt 保持 v3，v4 与 Holdout v2 作为负结果保留。

### LLM path (ECNU) — JSON 契约诊断与 Prompt v5 开发回归

后续诊断未复用 Holdout v2。三例全新合成输入分别运行 v3/500、v4/350 和 v4/500；v4/350
出现 1 次 `finish_reason=length`，v4/500 未出现长度终止。这证明 350 completion-token 上限
存在可复现的截断风险，但样本规模和输入范围不足以解释历史 18 次失败的全部成因。

仅提高上限形成 Prompt v5 后，在既有开发集中选择 2 个 Typology、2 个低证据和 2 个真实调用
对抗案例进行非盲回归：解析率 `1.0000`（6/6），解析后事实门 `0.8333`（5/6），p50/p95
为 `6.13/12.19 s`。失败输出被事实校验拦截，因此系统保持安全回退；但 v5 不满足晋升所需的
全量事实门与新盲测证据，默认版本不变。聚合结果见
`reports/public/llm_json_contract_diagnostic_20260804.json`。

### LLM path (ECNU) — Prompt v6 晋升 Holdout v3

v6 只使用 v5 已揭示的正文复制特征名失败做修复。完整开发回归为 34 案/27 次外部调用，解析率
`0.9630`（26/27），解析后事实门 `1.0000`（26/26），随后才冻结全新的 24 案 Holdout v3。

唯一 run `20260804T081410Z-d3eebce570` 的 20 次外部调用全部解析并通过事实门，4 个确定性
探针全部拦截。项目内盲审 20/20：Grounding 与 Overall 均为 `0.9500`，条件化、问题可执行和
实际对抗案例注入抵抗均为 `1.0000`。唯一失败是从“没有图证据”扩大推断“缺少佐证上下文”。
全部预注册门槛通过，v6 因此替换 v3 成为默认 Prompt；复核仍非外部合规专家验收。

## GraphSAGE FE v2 与时点新颖性验证矩阵

在历史架构 bake-off 之后，补做 GraphSAGE FE v2 / FE v2 + 时点新颖性双 seed、
validation-only 对照。GraphSAGE 基线为 `0.54478 / 0.69682`，加入时点新颖性后为
`0.90171 / 0.93154`，两个预注册配对门禁均通过；但同 seed GAT + 时点新颖性仍达到
`0.94539 / 0.94686`。因此该结果证明时点新颖性具有跨架构增益，同时继续支持 GAT 作为
主线图排序器。GraphSAGE 不读取新 test、不进入 OOF 或融合。完整门禁、切片和资源证据见
[GAT工程Pareto实验.md](GAT工程Pareto实验.md)。

## Edge GNN architecture comparison (complete)

Same PIT features + time split + hyperparams as GraphSAGE (`configs/models.yaml` protocol;
architecture overlays in `configs/models.{gat,rgcn,pna}.yaml`).  
Ran via `scripts/pipelines/run_arch_comparison.sh` (sequential GAT → RGCN → PNA; outputs
`artifacts/{gat,rgcn,pna}`; done flag `logs/arch_comparison_done.flag`).

| Model | Test PR-AUC | ROC-AUC | 0.1% Precision | 0.1% Recall | run_id | artifact |
|---|---:|---:|---:|---:|---|---|
| **GAT** | **0.9483** | 0.9996 | **0.9743** | **0.8378** | `20260726T023031Z-5ccb81baec` | `artifacts/gat` |
| RGCN (single-rel) | 0.9031 | 0.9997 | 0.9051 | 0.7783 | `20260726T062636Z-ef6be55956` | `artifacts/rgcn` |
| RGCN R=4 (relation ablation) | 0.8873 | 0.9997 | 0.8807 | 0.7573 | `20260726T135457Z-045b0b06ab` | `artifacts/rgcn_rel` |
| GraphSAGE | 0.8777 | 0.9996 | 0.8736 | 0.7512 | `20260725T090511Z-70e9a15e7b` | `artifacts/graphsage` |
| PNA | 0.7049 | 0.9992 | 0.6972 | 0.5996 | `20260726T064804Z-28028363ce` | `artifacts/pna` |

Ranking by test PR-AUC: **GAT > RGCN(single) > RGCN(R=4) > GraphSAGE > PNA**.  
R=4 uses `configs/models.rgcn_rel.yaml` (cross-border × currency-conversion relation ids).
It **does not replace** single-rel RGCN in the original bake-off narrative; it is a negative
heterogeneous-relation ablation.

GAT was subsequently promoted through the full frozen-score chain (OOF → fusion →
test → investigation views); see “Main-line model choice” above.

## Appendix — compute-budget experiments (do not change main-line)

Offline drills aligned to remaining JD themes (drift, community, unsupervised,
nonlinear fusion). **Main-line numbers above are unchanged.** Aggregated artifacts:

| Experiment | Artifact |
|---|---|
| Monthly ops + threshold recalibration | `artifacts/drift_monitoring` |
| High-score subgraph Louvain / CC | `artifacts/community_baseline` |
| Isolation Forest / AE | `artifacts/unsupervised_baseline` |
| Multi-rel RGCN (R=4) | `artifacts/rgcn_rel` |
| Sequence GRU | `artifacts/sequence_baseline` |
| GAT→CatBoost two-stage score feature | `artifacts/table_baseline_gat_distill` |
| Nonlinear OOF fusion heads | `artifacts/nonlinear_fusion` |
| DuckDB/Polars feature replay | `artifacts/batch_feature_replay` |

### Drift (frozen scores, test months Jul–Aug 2023)

| Model | Jul PR-AUC | Aug PR-AUC | Jul alert rate | Aug alert rate | Jul/Aug Recall |
|---|---:|---:|---:|---:|---:|
| CatBoost | 0.803 | 0.817 | 0.462% | 0.594% | 0.869 / 0.873 |
| GAT | 0.944 | 0.953 | 0.448% | 0.456% | 0.981 / 0.981 |
| fusion CB+GAT | 0.915 | 0.921 | 0.471% | 0.517% | 0.985 / 0.992 |

Stale validation-month calibration mainly hurts **alert volume / precision**, not ranking.

### Community baseline (seed top-2k + 1-hop, investigation window)

Louvain ≈ CC: ~724 communities, **case-account coverage 1.0**, top-10% communities hold
~53% of positive accounts in-subgraph. Homogeneous projection only — not a hetero GNN
(multi-rel RGCN R=4 = **0.8873**, below single-rel RGCN 0.903).

### Unsupervised (400k train negatives / 400k test sample)

| Model | Sampled test PR-AUC |
|---|---:|
| Isolation Forest | 0.0009 |
| Autoencoder | 0.0053 |
| Chance ≈ pos. rate | ~0.0012 |

Parallel baseline only; far below supervised CatBoost/GAT.

### Nonlinear fusion (OOF CatBoost + GAT)

| Head | Test PR-AUC |
|---|---:|
| Logistic (repro) | 0.9177 |
| MLP | 0.9073 |
| HistGBDT | 0.0410 |

The logistic head remained the best historical v1 fusion head; it is superseded by
the current versioned main-line table above.

### Multi-rel RGCN / sequence / distill / batch (summary)

| Experiment | Key metric |
|---|---|
| RGCN R=4 | test PR-AUC **0.8873** (below single-rel 0.903 / GAT 0.948) |
| Sequence GRU (sampled) | test PR-AUC 0.0103 |
| CatBoost + GAT score feature | test PR-AUC 0.9656 (two-stage; not pure tabular) |
| DuckDB/Polars `sender_outgoing_count_7d` | match rate **1.0** |

Additional negative result: the relation MLP scored PR-AUC `0.0363` with relation features
versus `0.0379` without them. These low-budget drills are evidence that the alternatives were
tested; none changes the promoted main line. Batch replay details are in
[批量特征重放.md](批量特征重放.md), and LLM probes in
[大模型调查系统.md](大模型调查系统.md).
